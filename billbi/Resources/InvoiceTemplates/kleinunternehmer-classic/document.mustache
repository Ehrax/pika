<!doctype html>
<html lang="de">
<head>
  <meta charset="utf-8">
  <title>{{invoiceNumber}}</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <main class="invoice-page">
    <header class="invoice-header">
      <section class="business-address">
        <h2>{{businessName}}</h2>
        <p>
          {{businessPersonName}}<br>
          {{{businessAddress}}}
        </p>
        <p>
          {{businessEmail}}<br>
          {{businessPhone}}
        </p>
        <p class="business-tax">
          {{#hasSenderTaxLegalFields}}
          {{#senderTaxLegalFields}}<span class="tax-label">{{label}}:</span> <strong>{{value}}</strong><br>{{/senderTaxLegalFields}}
          {{/hasSenderTaxLegalFields}}
          {{^hasSenderTaxLegalFields}}
          {{#taxIdentifier}}<span class="tax-label">Steuernummer:</span> <strong>{{taxIdentifier}}</strong><br>{{/taxIdentifier}}
          {{#economicIdentifier}}<span class="tax-label">Wirtschafts-IdNr:</span> <strong>{{economicIdentifier}}</strong>{{/economicIdentifier}}
          {{/hasSenderTaxLegalFields}}
        </p>
      </section>
      <section class="invoice-title">
        <h1>Rechnung</h1>
        <p>{{invoiceNumber}}</p>
        <dl class="invoice-date">
          <dt>Rechnungsdatum</dt><dd>{{issueDate}}</dd>
        </dl>
      </section>
    </header>

    <div class="header-rule"></div>

    <section class="recipient-section">
      <div class="recipient-address">
        <div class="section-label">Rechnungsempfänger</div>
        <h3>{{clientName}}</h3>
        <p>{{{billingAddress}}}</p>
        {{#hasRecipientTaxLegalFields}}
        <p class="business-tax">
          {{#recipientTaxLegalFields}}<span class="tax-label">{{label}}:</span> <strong>{{value}}</strong><br>{{/recipientTaxLegalFields}}
        </p>
        {{/hasRecipientTaxLegalFields}}
      </div>
    </section>

    <section class="intro-copy">
      Hiermit erlaube ich mir, für den folgenden Leistungszeitraum <strong>{{servicePeriod}}</strong> folgende Leistungen in Rechnung zu stellen:
    </section>

    {{> line-items}}

    {{> payment-details}}
  </main>
</body>
</html>
