# Sync Expense Evidence With the Workspace

Expense evidence should sync through the user's Apple cloud storage path along with expense metadata so receipts and invoices are available wherever the workspace is available. Evidence file availability is expected to be eventually consistent, so the UI should tolerate metadata arriving before files and show a syncing/loading state instead of treating the evidence as missing or broken.
